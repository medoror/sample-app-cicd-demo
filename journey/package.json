{
  "name": "journey",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^12.1.0"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "open": "start-server-and-test 'cd .. && yarn start' http://localhost:3000 'yarn cypress:open' ",
    "test": "start-server-and-test 'cd .. && yarn start' http://localhost:3000 'yarn cypress:run' "
  },
  "dependencies": {
    "start-server-and-test": "^1.15.2"
  }
}
